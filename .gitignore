# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# Raw exercise dataset dumps (source material — only the ~34 gifs actually used
# get copied into public/gifs/; the full ~140MB dataset doesn't need to be committed)
/archive/
/exercises-dataset-main/
*.zip